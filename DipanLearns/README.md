# DipanLearns

## Goal
Smallest React component example for JSX rendering.

## Core File Flow
`src/main.jsx -> src/dipan.jsx`

## Data Flow
Static values (`nickname`) are interpolated in JSX and rendered directly.

## Connectivity
`App.jsx` also renders `Dipan`, but `main.jsx` currently mounts `Dipan` directly.
